[project]
name = "langsmith-sdk-dev"
version = "0.0.0"
description = "Local dev harness for langsmith-sdk examples"
requires-python = ">=3.10"
dependencies = [
    "langsmith",
]

[tool.uv.sources]
langsmith = { path = "python", editable = true }
